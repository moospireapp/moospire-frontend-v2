import { Golos_Text, Satisfy } from "next/font/google";

export const golosText = Golos_Text({
  subsets: ["latin"],
  weight: ["400", "500", "700", "900"],
});

export const satisfy = Satisfy({
  subsets: ["latin"],
  weight: ["400"],
});
